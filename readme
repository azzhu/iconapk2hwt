# create by bigzhu
# qq:228812066
# 2018.2.24


喜欢许多第三方桌面的图标包，但是它们都不支持华为桌面怎么办？
本工具提供一种方法来编辑现有的hwt文件，更改其中的图标为图标包中的图标。

本工具是用来干什么的？
将图标包中的图标提取到已存在的华为主题文件中，也就是说更换现有主题包(.hwt)中的图标为图标包(.apk)中的图标。
若自己喜欢的图标没有被提取过去，则需编辑ind.txt文件，教程详见ind.txt文件。

使用方法：
1，将图标包apk放到根目录，重命名为icon.apk;
2，将需要更改的hwt(一定要是免费的华为主题包)文件放到根目录，重命名为theme.hwt;
3，执行iconapk2hwt.py文件;
4，执行后的输出文件为theme_new.hwt。